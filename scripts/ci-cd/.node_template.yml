# NodeJS job template

.node template:
  image: node:18
  cache:
  - key: "pnpm-$CI_COMMIT_REF_SLUG"
    paths:
      - frontend/claimlink_dashboard/node_modules/
  before_script:
    - npm install -g pnpm
    - cd frontend/claimlink_dashboard
    - pnpm install --frozen-lockfile
