# ClaimLink Environment Variables

# Canister IDs for ClaimLink
VITE_CLAIMLINK_CANISTER_ID=your-claimlink-canister-id
VITE_NFT_CANISTER_ID=your-nft-canister-id
VITE_CERTIFICATE_CANISTER_ID=your-certificate-canister-id
VITE_LEDGER_CANISTER_ID=your-ledger-canister-id

# IC Host (use localhost:4943 for local development, https://ic0.app for mainnet)
VITE_IC_HOST=https://ic0.app

# NFID Configuration for localhost development
# Comma-separated list of URLs that NFID should trust for signing
VITE_NFID_LOCALHOST_TARGETS=http://localhost:3000,http://localhost:5173,http://localhost:4943

# NFID Derivation Origin for localhost (use your actual domain in production)
VITE_NFID_DERIVATION_ORIGIN=http://localhost:5173

# For local development, uncomment and modify these:
# VITE_IC_HOST=http://localhost:4943
# VITE_NFID_DERIVATION_ORIGIN=http://localhost:5173
