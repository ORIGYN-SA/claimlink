/**
 * Template Context Hooks
 *
 * Re-export hooks from context for cleaner imports.
 */

export {
  useTemplateContext,
  useFieldValue,
  useFileArray,
  useAssetUrl,
  useTemplateLanguage,
  useLocalizedText,
} from '../context/template-context';
